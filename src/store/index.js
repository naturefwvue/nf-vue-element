import { createStore } from 'vuex'
import metaForm from './module/metaForm.js'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    form: metaForm // 表单控件的meta模块
  }
})
